{
  "case": "validate-severity-field",
  "check": "schema",
  "kind": "hard_rule",
  "schema": "validate.schema.json",
  "base": "prd:§7:validate",
  "mechanism": "additionalProperties:false",
  "contract_ref": "B.2·D126",
  "what": "검증 결과에 심각도를 실을 수 없다 — verdict는 validated/not_validated 둘뿐이다",
  "patch": [
    {
      "op": "set",
      "path": [
        "severity"
      ],
      "value": "critical"
    }
  ]
}
