{
  "case": "report-judgment-in-zone-note",
  "check": "schema",
  "kind": "hard_rule",
  "schema": "report.schema.json",
  "base": "prd:§7:report",
  "mechanism": "pattern(공유 사전)",
  "contract_ref": "D67·D127",
  "what": "자유 텍스트에 판정 어휘를 넣을 수 없다 — 렌더러는 JSON의 순수 함수라 그대로 표시된다 (zone_note)",
  "patch": [
    {
      "op": "set",
      "path": [
        "components",
        0,
        "zone_note"
      ],
      "value": "구조가 나쁨 — 즉시 고칠 것"
    }
  ]
}
