/**
 * <b>일부러 하드룰을 어기는 예제들.</b> 절대 main 소스셋으로 옮기지 않는다.
 *
 * <p>규칙이 통과만 하면 그 규칙이 살아 있는지 알 수 없다 — 범위가 비었을 수도, 술어가
 * 아무것도 고르지 않을 수도 있다. 여기 클래스들은 각 규칙이 <i>실제로 잡는지</i>를 보이는
 * 반례이며, {@code JqradarArchRulesTest}가 "이 패키지에 걸면 반드시 실패한다"를 단언한다.
 *
 * <p>{@code contract/schema/}의 변조 케이스와 같은 논리다(D124): 전부 통과하는 검사는
 * 아무것도 지키지 않는다.
 */
package io.jqradar.arch.violations;
